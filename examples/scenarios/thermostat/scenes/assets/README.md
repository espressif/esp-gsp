# Climate artwork

`dial.svg`, `climate.svg`, `snowflake.svg`, `leaf.svg` and `moon.svg` are original
vector artwork authored for this example, under the repository license.
They contain geometry and strokes, not UI text. Labels, values, ring state,
climate selection and the small procedural pulse remain native GSP controls.
