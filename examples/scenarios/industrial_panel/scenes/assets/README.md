# Industrial artwork

`pneumatic.svg` is original retained vector artwork created for this example
under the example SPDX license. It illustrates a filter with drain, pressure
gauge, directional valve and linear actuator as an HMI circuit.
`production_cell.png` is generated production-cell artwork; `scanner.gif` is a
loop used for the live link indication.
